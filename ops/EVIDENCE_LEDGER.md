# Evidence Ledger

The quantitative record of **why confidence changed**. Every meaningful observation gets a stable
ID; confidence-register movements and decision-log entries **cite evidence IDs instead of prose**, so
any belief in the program can be traced back to the specific evidence that justifies it.

**Classification:** `Engineering` · `Verification` · `Measurement` · `Data-quality` · `Methodology` ·
`Scientific`. **Confidence impact** uses ↑ / ↓ / → against the affected Confidence-Register
component(s). An entry is a *fact on the record*; whether it promotes anything still requires the
stopping rules (a ledger entry is never a Finding).

| ID | Date | Evidence | Classification | Confidence impact | Affected components | Refs |
|---|---|---|---|---|---|---|
| E-001 | 2026-07-06 | FanDuel live-flag bug fixed (read `inPlay` on the market, not the event); live FanDuel quotes captured for the first time | Engineering | ↑ Collector, ↑ Feed quality | Collector, Feed | collector v1.1 |
| E-002 | 2026-07-06 | Re-arm SPOF fixed: re-arm moved into an `always()` step so a platform cancellation no longer stops collection | Engineering | ↑ Collector | Collector | EP-2, ED-6, GD-1 |
| E-003 | 2026-07-09 | 11 consecutive re-arms (#16→#27), 0 failures, verified against the GitHub Actions API; continuous 3-day checkpointing | Verification | ↑ Collector | Collector | EP-2 |
| E-004 | 2026-07-09 | Integrity independently reproduced at 103,494 rows: 0 malformed / missing / duplicate / future-ts (under the tool's field definition) | Verification | → Integrity (maintained) | Integrity, Dataset | ED-4 |
| E-005 | 2026-07-09 | FanDuel densified (~1k→~10k live quotes); cumulative median sync lag collapsed 640→30 s — confirmed as a forward-fill artifact, not a market change | Measurement | ↑ Measurement (understanding), → Science | Feed, Synchronization | EP-1 |
| E-006 | 2026-07-09 | SR-1 sync sub-gate is quantized to {0}∪[30 s,∞) by the 30 s poll interval; a PASS at median 0 certifies collector co-capture, not <15 s market contemporaneity | Methodology | ↓ Protocol (gate validity) | Protocol, SR-1 | RD-1, ED-2, GD-3 |
| E-007 | 2026-07-09 | Pinnacle is stillborn — 6 pregame rows in one burst, 0 live quotes ever; root cause unknown | Engineering / Verification | ↓ Feed quality; holds Paper 2 readiness Low | Feed, Paper 2 readiness | ED-1, RD-2, KU-1 |
| E-008 | 2026-07-09 | bovada emits no `marketStatus` on any of 27,988 rows; OPEN/SUSPENDED/REMOVED is FanDuel-only (single-book status) | Data-quality | ↓ Feed quality | Feed, Safeguards | RD-4, ED-5 |
| E-009 | 2026-07-09 | 61 FanDuel rows (36 live) carry null odds and pass the integrity gate (odds not a required field); odds heavy-tailed | Data-quality | ↓ bounds the "integrity clean" claim | Integrity | RD-5, ED-4 |
| E-010 | 2026-07-09 | `book_panel` interleaves 2–3 alternate total lines per (game,book,ts) with no main-vs-alt discriminator (~95% of groups) | Data-quality / Methodology | ↓ threatens line-based inference | Feed, SR-1 pairs | RD-3, ED-3 |
| E-011 | 2026-07-09 | Adversarial audit: live panels are 100% July (0/103,494 June rows), temporally/book/data-type disjoint from Paper 1's sample | Verification | → Paper 1 (unchallenged, confirmed) | Paper 1 | — |
| E-012 | 2026-07-09 | 576 nominal cross-book "arbs" dissolved under scrutiny (median divergence 3.6 pp; concentrated in near-settled / suspended / status-unverifiable legs; same-poll co-presence, not executable) | Verification / Rejected | → Science (no evidence of inefficiency) | — | A-01 |
| E-013 | 2026-07-11 | Re-arm chain now 22 consecutive clean re-arms (#16→#38), 0 failures since the pre-fix #14; continuous ~15-min checkpoints across 07-09→07-11 (verified via Actions API) | Verification | → Collector (High, firmer) | Collector | EP-2 |
| E-014 | 2026-07-11 | Cumulative SR-1 median sync lag moved **30 s (07-09) → 91 s (07-10/11)** — non-monotonic; a further symptom of the implementation-dependent metric, not a synchronization regression | Measurement / Methodology | ↓ confidence in the sync-lag metric (not the system) | Synchronization, SR-1 | RD-1, E-006 |
| E-015 | 2026-07-11 | SR-1 overlap-games broke its plateau: 30 → 37 (07-10) → 45 (07-11) as new games enrolled | Dataset / Verification | ↑ Dataset maturity | Dataset, SR-1 | EP-4 |
| E-016 | 2026-07-19 | **Falsification of the book-heterogeneity claim.** Controlling for RD-3 (isolate the balanced main line) and sampling density (both books poll at ~30 s): the *update-frequency* difference **survives** — FanDuel re-prices its main line **4.7× more often** than Bovada (53/60 games; 92 s vs 916 s median between changes). The *pricing-tightness* claim **fails** — my v0.1 "FanDuel more internally consistent (vig IQR 0.36 vs 1.80 pp)" was an alt-line artifact; on the main line Bovada is tighter (0.05 vs 0.31 pp IQR). *(Magnitude superseded by E-017.)* | Measurement / Instrumentation | ↑ confidence that the books differ **behaviorally** (frequency); ↓ confidence in the specific pricing-consistency characterization (self-correction) | Book heterogeneity, Cross-book inference | RD-8, RD-3, A-10, GD-10 |
| E-017 | 2026-07-19 | **Attack on Assumption #4 (main-line extraction is well-defined).** Three reasonable "main line" definitions (balanced-odds, modal anchor, median line) **agree only 28.6%** of the time. E-016's frequency *direction* survives all three (FanDuel ≥ Bovada) but its **magnitude does not**: 4.7× (balanced) / **1.1× (modal anchor)** / 9.5× (median). The strong "4.7×" is a balanced-odds flitting artifact; under the anchor definition the books re-price at nearly the same rate. Vig-tightness reversal (Bovada tighter) holds under balanced+modal, not median. | Measurement / Methodology | ↓ confidence in the *magnitude* of book heterogeneity; ↑ confidence that extraction choice is a live confound. Assumption #4 **refuted** as a definition-invariant operation. | Main-line extraction, Book heterogeneity, all cross-book inference | E-016, RD-3, A-10, A-11 |
| E-018 | 2026-07-19 | **Attack on Assumption #2/#5 (is cross-book leadership identifiable from 2 books at 30 s?).** Matched main-line transition events (1,019 across 60 games): naive result "FanDuel leads +92 s (74%)" is frequency-confounded, so ran a base-rate placebo — a matching FanDuel move precedes a Bovada move **36%** vs follows **18%** (+17 pp, 2:1); the reverse test is negative (Bovada leads FanDuel −6 pp). A frequency artifact would be time-symmetric (~0), so the directional signal **survives**. **Invariant to main-line definition** (modal ≡ balanced, identical output), which is why A-11 is frozen. **NOT separable from feed latency vs information** (open); game-clustered n, single 16-day window, SR-1 still BLOCKED. | Scientific (identifiability) — *methodological feasibility only, NOT a gated finding* | ↑ confidence that leadership is **identifiable** (contra the prior presumption 2 books couldn't); **no** market-efficiency claim; stays behind SR-1 | Cross-book leadership, Paper 2 estimand, SR-1 | A-11 (frozen), A-12, RD-1, RD-8 |
| E-019 | 2026-07-28 | **SR-1 Criterion 3 (overlap games) CLEARED: 102/100** (independently recomputed from the raw panels, not the collector self-report). Three of four SR-1 criteria now pass: pairs 51,146/2,000 ✅, sync lag 0.0 s ✅, overlap games 102/100 ✅, **books quoting live 2/3 ⬜**. Gate overall 91% and **still BLOCKED**. The third book is now the *sole* remaining blocker, arriving on the schedule flagged 2026-07-26. Pinnacle dead 22.5 days. | Dataset / Verification (a gate state change, **not** a finding) | ↑ Dataset maturity. **No** confidence in any scientific claim moved; no analysis authorized or run. | SR-1, Dataset, Cross-book capability | GD-9, GD-12, E-015, RD-2 |
| E-020 | 2026-08-04 | **Feed provenance probe, first read (11,981 fetches, coverage satisfied in both pregame and live).** Two results. (1) **Transport is NOT common-mode across books.** Median HTTP `Date` staleness is **0.52 s for bovada** (tight, unquantized) versus **30.93 s for fanduel**, and the fanduel distribution is **quantized at multiples of ~31 s** (10 fetches near 0, 5,816 near 31, 116 near 62), the signature of a cached response rather than clock skew or network jitter. A ~31 s book-specific offset dwarfs any plausible pricing-latency difference. (2) **bovada exposes `[].events[].lastModified`**, 266 distinct values over 5,990 fetches: a genuine publication-clock candidate, not yet shown to track price revisions. fanduel exposes none (0/5,991; 95% upper bound 0.050%). **Caveat:** `Date` records when the *response* was generated, possibly by a cache, not when the *price* was set, so this bounds observation freshness rather than the pricing-to-publication gap. | Measurement / Instrumentation (Condition 3 evidence, NOT a market finding) | ↓ confidence that A4 can hold as stated (common-mode is refuted, measured); ↑ confidence that the transport component is at least *measurable*, which points at Outcome B rather than C | A4, Condition 3, Paper 2 estimand | GD-18, RD-8 |
| E-021 | 2026-08-07 | **Per-market provenance panel, first read — the four pre-registered GD-19 questions, all answered.** 8,463 market transitions over one slate (bovada 4,847 / fanduel 3,616). **(Q1) YES but non-discriminating:** bovada's `lastModified` moved on 1,094/1,094 price changes — and also on **98.6%** of transitions where the price did *not* move. It is an event-level heartbeat, so near-certain coincidence with price moves carries almost no information. **(Q2) NO:** 0/1,094 price changes left the clock frozen; 95% upper bound **0.27%** (rule of three). The clock is therefore a *necessary* condition for a revision — usable to exclude revisions, not to date them. **(Q3) YES, completely:** on cache hits the residual `recv − Date − Age` is 0 s for **100%** of 3,500 fetches; on the 116 cache **misses** there is no `Age` header and **no offset at all** (recv−Date median 0.0 s). The ~31 s is CDN cache age — not clock skew, not transit, not bookmaker behaviour. For bovada the convention **inverts**: `Date` is rewritten at the edge (recv−Date ≈ 0) while `Age` reports objects up to ~549 s old, so `Date` is uninformative there and `Age` is the only staleness signal. **(Q4) NO:** `marketTime` equals `openDate` on 3,615/3,616 rows, is constant within a market, and survived 3,494/3,495 price revisions untouched — a contract clock (scheduled first pitch), not a publication stamp. **Ordering defect discovered while answering Q1/Q2:** **28.4%** of bovada `lastModified` transitions arrive **backward in time** (1,348 of 4,749), and backward jumps carry a higher `Age` **84%** of the time (median **+240 s**) versus 35% / −1 s for forward jumps — the CDN serves objects of differing age, so a later poll can return an older market state. Newly-published stamps reach us at p50 **247 s** (p90 655 s) old. | Measurement / Instrumentation (Condition 3 closure, **NOT** a market finding) | ↓ confidence that a usable publication clock exists on either book (bovada's is out-of-order and minutes stale; fanduel's is absent); ↑ confidence that **observation latency is measurable and book-specific**; → no scientific claim moved | A4, Condition 3, Paper 2 estimand, Outcome A/B/C | GD-19, GD-18, E-020 |
| E-022 | 2026-08-10 | **Collector outage, 101.8 hours, silent.** Collection stopped at 2026-08-06T16:02 and was not detected until this review. Root cause established from the run log, not inferred: run #150's self-re-arm dispatch returned **HTTP 500** (`{"message":"Failed to run workflow dispatch","status":"500"}`) — a transient GitHub API error. The step was `curl -s … \|\| true` with **no status check and no retry**, so it printed `re-armed after 208 min`, the step concluded **success**, the job concluded normally, and the workflow remained `active`. Every available health signal reported healthy while the chain was dead. **Data lost: ~4.2 days of live slates** (no `book_panel` rows between 08-06T15:57 and 08-10T21:53). Fixed in two places: the re-arm now requires HTTP 204, retries 5× with backoff, and exits non-zero so a failure is visibly red; and a new hourly **watchdog workflow** on a separate concurrency group relaunches the collector whenever no live run is in flight, bounding any future outage to ~1 h. Collector restarted 2026-08-10T21:53 (run #151). | Engineering (infrastructure failure + fix) | ↓↓ confidence in **self-reported** collector health (the metric was structurally incapable of reporting this failure); ↑ confidence in collector continuity going forward (external observer added); → no scientific claim moved; dataset has a documented 4.2-day gap | Collector, Dataset continuity, Integrity | E-002, GD-1, EP-2 |
| E-023 | 2026-08-10 | **Second, previously undocumented collection gap discovered: 2026-07-12T22:56 → 2026-07-15T07:34 (~56.6 h).** Found while verifying E-022, by enumerating distinct calendar days in `book_panel.jsonl` — calendar days 07-13 and 07-14 are absent entirely. **Mechanism not established** (it predates the run-log review that diagnosed E-022). Unlike E-022 this gap **did truncate live data**: collection stopped at 22:56 UTC mid-slate with three games holding live quotes in the final hour (`TOR@SD`, `COL@SF`, `ARI@LAD`), so those games are partially observed. Consequence for the July analyses (E-016/E-017/E-018): missing games reduce coverage without a plausible bias route, but the three truncated games contribute shortened observation windows to per-game interval statistics, an effect **not quantified**. Recorded as an additional qualification on those entries, not as an invalidation — E-017 had already established that the magnitude of that result is not robust to the main-line definition. | Data-quality | ↓ confidence in the completeness of the July window; → E-016/E-017/E-018 carry one further unquantified qualification; no scientific claim moved | Dataset continuity, Integrity, Book heterogeneity | E-022, E-016, E-017, DATA_CONTINUITY.md |
| E-024 | 2026-08-10 | **SR-1's synchronisation criterion was repaired, and it now fails.** The gate certified synchronisation with `median sync lag < 15s`, computed as the spread between the two books' quote timestamps at a common instant. That statistic measures **when our collector captured** the quotes, not whether they describe the same market moment; because both books are read in one poll it sat pinned at **0.0 s** and the criterion passed unconditionally (E-006, E-014 flagged the quantization; the metric was never repaired). E-021 supplied the missing input: delivered staleness is book-specific, so two quotes captured in the same poll can describe market states minutes apart. The criterion is now a **contemporaneity bound** — co-capture lag plus the two books' 90th-percentile delivered staleness — computed from the `Age` header in the provenance panel. Measured: bovada median 47 s / p90 **548 s** (n=5,236), fanduel median 30 s / p90 31 s (n=4,484), co-capture lag 0.0 s, **bound = 579.0 s against a 15 s target — FAIL**. Consequence: SR-1 moves from **91% with 3 of 4 criteria passing** to **67% with 2 of 4**. **The third book was never the sole remaining blocker.** Synchronisation was also failing for the entire period it was reported as passing; the old statistic concealed it. Pairs 79,502/2,000 ✅, overlap games 144/100 ✅, contemporaneity bound 579/15 ❌, books quoting live 2/3 ❌. | Methodology / Measurement (a gate-validity correction, **not** a finding) | ↓↓ confidence in the prior SR-1 progress figure (91% was an artifact of a metric that could not fail); ↑ confidence in the gate's validity going forward; → no scientific claim moved | SR-1, Protocol, Synchronization, Cross-book inference | E-006, E-014, E-019, E-021, RD-1, GD-3 |
| E-023a | 2026-08-11 | **E-023 sensitivity check — the three truncated games carry none of the July results.** Reran the load-bearing statistic of E-016/E-017/E-018 under three samples: ALL (108,593 live July rows), DROP-SLICE (drop only the truncated 07-12 slices; 107,079), DROP-MATCHUP (drop those matchups entirely including their complete earlier dates; 104,170). *Note:* `game` is a **matchup string, not a unique game id** — ARI@LAD appears on both 07-11 and 07-12 — so only the 07-12 slices are truncated and DROP-MATCHUP is deliberately over-aggressive. Result: **every statistic is stable to three significant figures** across a ~4% change in sample size (frequency ratio 19.68× in all three; vig IQR 0.58/0.15 pp in all three; placebo gap +1.4/+1.4/+1.5 pp). No direction, magnitude, or gate changes. **No gate is affected** in any case: SR-1 depends on pair counts, overlap games and the contemporaneity bound (E-024), none of which these matchups determine. **E-023 is a documented data-continuity qualification, not a new research branch.** | Verification / Data-quality | → E-016/E-017/E-018 unchanged by the truncation; ↑ confidence that E-023 is operational rather than methodological | E-023, E-016, E-017, E-018 | GD-20, e023_sensitivity.py |
| E-025 | 2026-08-11 | **Reproduction failure: the July analyses cannot be reproduced from the committed code, and the alt-line problem is pregame-only.** While running E-023a, an independent reconstruction of E-016/E-017/E-018 matched **0 of 7** recorded figures: frequency ratio 19.7× vs recorded **4.7×**; bovada median gap 610 s vs **916 s**; fanduel 31 s vs **92 s**; vig direction *fanduel tighter* vs recorded *bovada tighter*; three-rule agreement 100% vs **28.6%**; placebo precede 76% vs **36%**, follow 74% vs **18%**. The probable cause is a sample-definition difference, and it is measurable: **0.0%** of *live* (game, book, ts) groups carry more than one posted line, whereas **54.1%** of *pregame* groups carry two. RD-3/E-010 recorded ~95% of groups carrying 2–3 alternate lines; **that structure exists only in the pregame rows**. In a live-only sample every extraction rule selects the same quote, so E-017's invariance question does not arise and its 28.6% disagreement cannot be a live-panel statistic. **No E-016/E-017/E-018 number is retracted here** — the original analysis code was not recovered, so the discrepancy is recorded as open, not resolved. What is established independently is the panel-structure fact. | Data-quality / Verification (**reproducibility gap**, not a finding) | ↓↓ confidence that E-016/E-017/E-018 as recorded describe the live panel; ↑ confidence in the live panel's structure (single posted line per poll); → no scientific claim promoted or retracted | E-016, E-017, E-018, RD-3, A-11, E-010 | E-023a, e023_sensitivity.py |
| E-025r | 2026-08-11 | **E-025 RESOLVED: E-016/E-017/E-018 recovered verbatim and reproduced from committed code.** The original implementations were recovered from the session transcript (they were ad-hoc heredocs, never committed) and transcribed into `july_analyses.py` **without reinterpretation**, preserving each questionable choice under a `CAVEAT` comment rather than correcting it. **Reproduction at the record's as-of date (`< 2026-07-19`): 6 of 7 numeric checks match exactly** — bovada 916 s ✓, fanduel 92 s ✓, vig IQR 0.05 vs 0.31 pp with bovada tighter ✓, balanced 4.7× ✓, modal 1.1× ✓, median 9.5× ✓, E-018 36%/18% ✓, and `53/60 games` reproduces exactly. The lone residual is E-017's agreement rate, 28.2% here vs 28.6% recorded; the `< 2026-07-20` cutoff gives 28.8%, so the recorded value is bracketed and the original evidently ran mid-day 07-19 on a partial day. **The earlier reconstruction failure (E-025) had two causes, both now established and neither an error in the recorded entries:** (1) **sample growth** — the panel is append-only and now runs through 07-31, and the same code returns 609 s where the record says 916 s purely because the sample is larger; every historical figure must be regenerated at its own as-of date, now enforced by `--asof`; (2) **the reconstruction filtered to live rows** while E-016 and E-017 group over the **entire panel including pregame**, which is where RD-3's alternate lines exist (54.1% of pregame groups carry two lines; **0.0%** of live groups do). E-018 is live-only and matches moves on the **same line level**. **No recorded figure is retracted or amended.** | Verification / Reproducibility | ↑↑ confidence that E-016/E-017/E-018 describe what they claim; ↑ confidence in the reproducibility standard now that it is demonstrated rather than asserted; E-025 closed | E-016, E-017, E-018, E-025, RD-3, A-11, Paper 2 §6.6 Condition 1 | july_analyses.py, GD-20 |
| E-023b | 2026-08-11 | **E-023 sensitivity, rerun against the authoritative implementation — supersedes E-023a.** At the record's as-of date, dropping the three truncated games changes nothing material. Under **DROP-SLICE** (the precise correction, removing only the 07-12 slices): balanced ratio **4.7× → 4.7×**, `53/60 games` unchanged, vig IQR 0.05/0.31 pp unchanged, E-018 **36%/18% (+18 pp)** unchanged, reverse test −7 pp unchanged. Under **DROP-MATCHUP** (deliberately over-aggressive, discarding those matchups' complete earlier dates too): 4.3× / 50-57 games / vig 0.05 vs 0.38 pp / E-018 35%/18% (+17 pp) / reverse −6 pp — every direction and every qualitative conclusion preserved. **The three truncated games carry none of the July results, now demonstrated on the implementation that reproduces the record rather than on a reconstruction.** E-023 is a documented data-continuity qualification. No gate is affected: SR-1 turns on pair counts, overlap games and the contemporaneity bound (E-024). | Verification / Data-quality | → E-016/E-017/E-018 unaffected by the truncation, now authoritatively; E-023 closed as operational | E-023, E-023a, E-016, E-017, E-018 | july_analyses.py, E-025r |

| E-026 | 2026-08-17 | **Silent persistence failure: 79 hours of collection gathered and discarded, with every signal green.** Collection did **not** stop. Runs cycled normally, the hardened re-arm worked (`re-armed after 334 min (attempt 1)`), and the run log at 08-17T21:17 reports `book_panel.jsonl: 679,911 rows, last 21:13:15`, both books quoting 3.3 min ago, `HEALTH_OK: yes`, integrity clean — against **672,035 rows on the branch** (~7.9k unpushed at that reading). The data never left the runner. **Mechanism: GitHub hard-rejects any file over 100 MiB (104,857,600 bytes).** `book_panel.jsonl` grew into that ceiling — the last checkpoint that landed left it at **104,857,591 bytes, nine bytes under the limit** — so every subsequent push was rejected, and `-q` plus the `&&` chain swallowed the rejection. Verified by tracing blob size across the final checkpoints (104,793,631 → … → 104,857,591, then nothing). Abrupt rather than gradual because the limit is a cliff: below it every push succeeds, above it every push fails. *(Cause corrected 2026-08-18: this entry first blamed a dirty worktree defeating `git pull --rebase`, and a fix shipped on that basis. That diagnosis was wrong — the pull was never the failing step. `--autostash` is retained as correct hygiene but fixed nothing.)* **Each ~5.5 h run started from the 08-14 branch state, gathered its own ~8k-row window, and discarded it on exit; successive runs covered different windows, so the aggregate loss is the full 79 h and none of it is recoverable.** *(Corrected 2026-08-18: this entry first said 594,643 rows on the branch and ~85k lost per run. 594,643 was a stale local count taken before fetching the 08-14 checkpoints; the branch held 672,035. The finding is unchanged.)* **Monitoring failed twice:** the checkpoint's exit status was independent of whether data landed, and the watchdog added after E-022 asked *"is a run in progress?"* — which stayed **yes** for three days, so it stood down hourly exactly as designed. **Fixed:** checkpoint uses `--autostash`, captures both outputs, and emits `::warning`/`::error`; watchdog now checks the branch's last commit age and goes red past 180 min, without relaunching (a restart would discard unpushed rows). | Engineering (infrastructure failure + fix) | ↓↓ confidence in **activity-based** health signals (a third consecutive failure whose monitor reported healthy); ↑ confidence in outcome-based monitoring; → no scientific claim moved; dataset has a third documented gap | Collector, Dataset continuity, Integrity | E-022, E-023, DATA_CONTINUITY.md Gap 3 |
## How the ledger is used

- **Confidence Register** movements cite the E-IDs that justify them; a level never moves without a
  ledger citation. (Existing register basis text is being migrated to E-ID citations as it is next
  touched — new movements cite E-IDs from the outset.)
- **Governance Decision Log** entries cite the evidence they acted on.
- **Inference Graph** edges cite E-IDs on the path from observation to any paper claim.

## Discipline

An entry records evidence, not a conclusion. Classification is about the *kind* of evidence, not its
importance. A `↑ Measurement, → Science` impact (e.g., E-005) is the common and correct shape: the
instrument got better, the science did not move. Never log an interpretation as evidence.
