# The Third Turn — REPRODUCTION LOCK
#
# Exact dependency set verified on 2026-08-22 to reproduce the committed artifacts
# of Paper 1 and Paper 2 from this repository. Use this file, not requirements.txt,
# when the goal is replication.
#
#   requirements.txt       — the RUNTIME set for the live collector (floors only,
#                            deliberately loose; includes pybaseball/pyarrow/streamlit,
#                            none of which are needed to reproduce either paper).
#   requirements-lock.txt  — THIS FILE. The REPLICATION set: exact pins, no extras.
#
# Interpreter: Python 3.11.15
#
# Verified with this set (see paper/REPRODUCE.md for the commands):
#   * tests/                       -> 113 passed
#   * output/encompass.json        -> byte-identical
#   * output/calibration.json      -> byte-identical
#   * output/program_a.json        -> byte-identical
#   * paper/figures/*.png (21/21)  -> byte-identical
#   * paper/paper1.pdf, paper2.pdf -> build successfully; NOT byte-identical
#                                     (the PDF writer embeds a creation timestamp;
#                                     size and content match)
#
# Install:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r the_third_turn/requirements-lock.txt
#
# The PDF build additionally requires Chromium on PATH or at /opt/pw-browsers/chromium.

aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-types==0.8.0
attrs==26.1.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
frozenlist==1.8.0
idna==3.11
iniconfig==2.3.0
kiwisolver==1.5.0
Markdown==3.10.3
markdown-it-py==4.2.0
matplotlib==3.11.1
mdurl==0.1.2
multidict==6.7.1
numpy==2.4.6
packaging==24.0
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
propcache==0.5.2
pydantic==2.13.4
pydantic-core==2.46.4
Pygments==2.21.0
pyparsing==3.1.1
pytest==9.1.1
python-dateutil==2.9.0.post0
rich==15.0.0
scipy==1.17.1
six==1.16.0
tqdm==4.70.0
typing-extensions==4.16.0
typing-inspection==0.4.4
yarl==1.24.5
